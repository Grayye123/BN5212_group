# 协作和结果接收

## 最小工作流程

1. 在 Issue 写清一个任务：要回答什么、允许使用什么、交付哪几个文件。领取时写明负责人，避免两个人同时改同一处。
2. 从当前 `main` 开分支。将起始 commit 和 Issue 链接记入结果说明。一个分支只处理这一项任务。
3. 把 README、agent.md、HANDOFF 和 Issue 交给自己的 GPT / Codex。当前先做维护；后续涉及代码或运行时，再明确其执行范围和能力。
4. 按 [模板](templates/RESULT.md) 保存结果说明。代码、依据表、配置和适合共享的汇总结果一起提交，再发起 PR。
5. 将 PR 链接交给维护者。维护者取回文件，核查后合入 `main`，更新结果记录和交接。后续工作从更新后的 `main` 开始。

Issue 用来安排工作；PR 用来提出具体文件修改并保留讨论与核查记录。[GitHub Issue 文档](https://docs.github.com/en/issues/tracking-your-work-with-issues/using-issues/creating-an-issue)、[PR 文档](https://docs.github.com/en/pull-requests/reference/pull-requests)

## 队友只有 ChatGPT 网页时

让 ChatGPT 输出一份可保存的 Markdown 说明和必要代码文件。队友可以在 GitHub 网页上传到自己的分支并创建 PR；不必先学会命令行。若暂时只能提供文字，把结果放到对应 Issue 的正文中，并附产物文件；维护者收到后再整理成 PR。不要只贴一句“GPT 说可行”或一个别人无权访问的聊天链接。

ChatGPT 的 GitHub 应用不能保证直接写入；官方说明它主要读取仓库，编辑推送可使用 Codex。GitHub 仓库权限和各自 ChatGPT 的连接授权均需就绪。[OpenAI 说明](https://help.openai.com/en/articles/11145903-connecting-github-to-chatgpt)

## 给 GPT 的开工提示

```text
请先读取 README.md、agent.md、HANDOFF.md 和本任务的 Issue。
只完成这项任务，保留已有方向与限制。胸片源文件暂不下载、不移动、不处理。
开始前说明你实际能访问哪些文件，以及能否运行代码。
以 templates/RESULT.md 为结构交付结果；没有运行就标明未运行。
所有结论带文件或文献依据，保留失败和不确定事项，不补造实验数字。
结束时列出需要维护者接收的文件及建议更新的交接内容。
```

## 维护者如何“捞回”成果

1. **定位**：读取 PR 的说明、文件差异、起始和当前 commit，确认对应 Issue；核实仍有权限。
2. **接收**：记录为“已收到”；保留原提交，不能因为文字看起来合理就标成已验证。
3. **核查**：文献工作逐条核对关键来源；代码先查看改了什么、会读写什么，再做合适的检查；实验需有配置、日志和汇总结果。逐病例预测留在获准的数据位置，仓库仅记录无凭据的证据编号与汇总。
4. **处理差异**：若队友基于旧版本工作，比较双方改动，保留冲突点及理由。不要覆盖后来修正的标签规则、数据划分或结果。
5. **采纳**：能支持结论才在结果登记中记“已验证”；缺少材料写“需补充”。合并说明采用了什么、做了什么验证、哪些尚未运行。
6. **交接**：更新 `HANDOFF.md`；改变研究方法时同时更新 `docs/DECISIONS.md`。未来任务引用已合入版本。

## 提交前的小检查

- 文件中无胸片、报告全文、逐病人/逐检查数据、医生映射、私人链接、凭据或聊天历史。
- 原始输入和已有结果未被覆盖；文件使用 UTF-8 和仓库内相对链接。
- 实验结果写明样本数、标签规则、病人划分、随机种子、代码版本、实际命令和环境；未完成项明确标出。
- 由维护者检查中文编码、文档链接和共享清单，再查看本次文件差异。文档检查不等于科学结论验证。

暂不创建定时轮询或自动合并；收到具体提交后按以上步骤接收即可。
