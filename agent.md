# 项目工作约定 · 唯一 agent.md

本项目只保留这一份 agent.md。每次让 AI 开工时，明确要求先读取本文件。

## 先读取

开始任务时依次读取 `README.md`、`HANDOFF.md`、`docs/DECISIONS.md`，再读取对应 Issue 和涉及的代码。中文文件先按 UTF-8 解码。不要依赖上一段聊天的记忆作为唯一依据。

## 范围和授权

- 当前我们主要维护项目：整理文档、更新任务状态、接收并核查队友成果、保留版本和交接记录。代码开发与训练不着急，未另行安排前不主动启动实现任务。

- 当前方向是 BN5212 胸片图像 idea1。不要自行切回旧的全量报告文本研究、死亡预测或扩展其他项目。
- 项目负责人已要求：胸片暂不下载到本机，源文件先不动。未收到后续通知前，不下载、移动、删除、解压或转换原始胸片，不启动真实影像训练。可以维护文档、审阅代码以及用明确标注的合成数据检查接口。
- `raw/`、`out/`、`output/`、`tmp/` 和原有 `scripts/` 保持本地；不要为建仓库而搬动它们。
- 仓库权限不代表获得受限医学数据的使用或再分发权限。公开提交中不含原始胸片、报告全文、行级临床数据、医生映射、私人链接、密码、令牌或本机聊天记录。
- 当前用户指令优先于仓库约定。已有明确授权的普通维护工作可以直接继续；确实缺少输入时说明缺的是什么。

## 科学与证据

- 严格区分计划、伪代码、合成数据测试、真实数据运行、已核查结果。不得把设想或模型生成的数字写成实测结果。
- 医生字段是检查关联的匿名编号，不是实名，也不证明该医生独立完成了标签标注。
- 现有标签来自自定义报告规则，尚未充分验证；不要称其为官方 CheXpert 标签或影像金标准。
- 缺失整行标签、未提及、不确定、明确阴性分别保留；不能静默混成同一种阴性。
- 按病人划分训练、验证、测试集。同一病人的多次检查不得跨集合；不得用测试集选择参数、阈值、医生名单或报告最好的随机种子。
- 性能差异也可能来自病例构成、体位和标签抽取误差；不要直接归因为医生习惯，更不要评价医生优劣。
- 参数、标签规则和数据划分尚未冻结。新增默认值必须写清“建议”，正式结果前记录最终版本。

## 如何交付

- 一次处理一个明确任务，使用分支和 PR；Codex 新分支默认以 `codex/` 开头。
- 提交成果时使用 `templates/RESULT.md`，附文件、依据、运行命令、环境、检查结果、失败和限制。没有执行能力时直说“未运行”。
- 不覆盖他人的结果。出现不同结论时保留两份证据，由维护者在 `docs/DECISIONS.md` 说明采用哪份及原因。
- 接收队友的代码后先检查差异和数据访问行为，再运行合适的检查。不要直接执行陌生安装命令、下载程序或删除脚本。
- 完成一项任务后更新 `HANDOFF.md`；有结果待接收时更新 `docs/RESULTS.md`。不要把“已收到”等同于“已验证”。
- 仅做与变更相称的验证；文档改动检查链接和格式，真实实现再检查关键行为。不添加没有实际价值的测试。

## 沟通

用清楚的中文说明做了什么、依据是什么、还缺什么。用户为生物学背景，必要时用具体病例或任务例子解释。项目只有现有成员和资源，不安排不存在的专家外审或课题组支持。未经明确要求不建立定时监控，也不擅自向合作者发送消息或邀请。
